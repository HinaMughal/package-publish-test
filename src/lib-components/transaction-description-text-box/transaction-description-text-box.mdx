<!-- Custom-MDX-Documentation.mdx -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { TransactionDescriptionTextBox } from "./transaction-description-text-box";

# `Transaction description text box`

This textbox is used to enter transaction description.

## Validation

1. Field is required.
2. DataType is AlphaSpecial.
3. MaxLength is 15.

## Attributes of Transaction description text box

Below is the list of Attributes.

<ArgsTable of={TransactionDescriptionTextBox} />{" "}

## Events Description

### TransactionDescriptionTextBox-onChange

The onChange event occurs when the user change in text box

### TransactionDescriptionTextBox-onBlur

The onBlur event occurs before focus lost.

### TransactionDescriptionTextBox-onKeyPress

The onkeypress event occurs when the user presses a key (on the keyboard).

### TransactionDescriptionTextBox-onKeyUp

The onkeypress event occurs when the user presses a up-arrow key (on the keyboard).

### TransactionDescriptionTextBox-onFocus

The onfocus event occurs when an element gets focus.The onfocus event is most often used with input and select.

### Demo

<Canvas>
  <Story id="transactiondescriptiontextbox--primary" />
</Canvas>
