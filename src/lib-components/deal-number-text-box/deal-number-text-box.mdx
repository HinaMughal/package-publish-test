<!-- Custom-MDX-Documentation.mdx -->
import { ArgsTable , Canvas , Meta ,Story } from "@storybook/addon-docs";
import { DealNumberTextBox } from './deal-number-text-box';

# `Deal number text box`

 
Deal Number is Text Box component which is used to set Deal Number 

## Validation 

1. Field is required.
2. DataType is numeric.
3. MaxLength is 7.

## Attributes of Deal number text box
Below is the list of Attributes.
<ArgsTable of={ DealNumberTextBox } /> 

## Events Description

### DealNumberTextBox-onChange

The onChange event occurs when the user change in text box

### DealNumberTextBox-onBlur

The onBlur event occurs before focus lost.

### DealNumberTextBox-onKeyPress

The onkeypress event occurs when the user presses a key (on the keyboard).

### DealNumberTextBox-onKeyUp

The onkeypress event occurs when the user presses a up-arrow key (on the keyboard).

### DealNumberTextBox-onFocus

The onfocus event occurs when an element gets focus.The onfocus event is most often used with input and select.
### Demo

<Canvas>
<Story id="dealnumbertextbox--primary" />
</Canvas>

 