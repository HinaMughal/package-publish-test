<!-- Custom-MDX-Documentation.mdx -->
import { ArgsTable , Canvas , Meta ,Story } from "@storybook/addon-docs";
import { CapturedByTextBox } from './captured-by-text-box';

# `Captured By text box`

Captured By Text Box Component is the name of user who captures signatures

## Validation 
- Maximum field length is 10
- Field is alphaNumeric

## Attributes of Captured By text box
Below is the list of Attributes.
<ArgsTable of={ CapturedByTextBox } /> 

## Events Description
Note:  `None of events will occur if component is disabled`
### CapturedByTextBox-onChange

The onChange event occurs when the user change in text box
### CapturedByTextBox-onBlur

The onBlur event occurs before focus lost.

### CapturedByTextBox-onKeyPress

The onkeypress event occurs when the user presses a key (on the keyboard).

### CapturedByTextBox-onKeyUp

The onkeypress event occurs when the user presses a up-arrow key (on the keyboard).

### CapturedByTextBox-onFocus

The onfocus event occurs when an element gets focus.The onfocus event is most often used with input and select.
### Demo

<Canvas>
<Story id="capturedbytextbox--primary" />
</Canvas>

 