<!-- Custom-MDX-Documentation.mdx -->
import { ArgsTable , Canvas , Meta ,Story } from "@storybook/addon-docs";
import { AmountTextBox } from './amount-text-box';

# `Amount text box`
This text box is used to display Amount.

## Validation 
1. Max Field length set 18 .
2. Restricted to 2 Decimal.
3. Field is Disabled.
4. field is required. 
5. DataType is numericDecimal


## Attributes of Amount text box
Below is the list of Attributes.
<ArgsTable of={ AmountTextBox } /> 

## Events Description

### AmountTextBox-onChange

The onChange event occurs when the user change in text box

### AmountTextBox-onBlur

The onBlur event occurs before focus lost.

### AmountTextBox-onKeyPress

The onkeypress event occurs when the user presses a key (on the keyboard).

### AmountTextBox-onKeyUp

The onkeypress event occurs when the user presses a up-arrow key (on the keyboard).

### AmountTextBox-onFocus

The onfocus event occurs when an element gets focus.The onfocus event is most often used with input and select.
### Demo

<Canvas>
<Story id="amounttextbox--primary" />
</Canvas>

 