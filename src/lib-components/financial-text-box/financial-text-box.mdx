<!-- Custom-MDX-Documentation.mdx -->
import { ArgsTable , Canvas , Meta ,Story } from "@storybook/addon-docs";
import { FinancialTextBox } from './financial-text-box';

# `Financial text box`
 
Financial is a Text Box Coomponent that defines if a user can performs financial transactions or not

## Validation  
- Maximum field length is 3
- Field is alphaOnly

## Attributes of Financial text box
Below is the list of Attributes.
<ArgsTable of={ FinancialTextBox } /> 

## Events Description
Note: `None of events will occur if component is disabled`
### FinancialTextBox-onChange

The onChange event occurs when the user change in text box

### FinancialTextBox-onBlur

The onBlur event occurs before focus lost.

### FinancialTextBox-onKeyPress

The onkeypress event occurs when the user presses a key (on the keyboard).

### FinancialTextBox-onKeyUp

The onkeypress event occurs when the user presses a up-arrow key (on the keyboard).

### FinancialTextBox-onFocus

The onfocus event occurs when an element gets focus.The onfocus event is most often used with input and select.
### Demo

<Canvas>
<Story id="financialtextbox--primary" />
</Canvas>

 