<!-- Custom-MDX-Documentation.mdx -->
import { ArgsTable , Canvas , Meta ,Story } from "@storybook/addon-docs";
import { ExpiryDateTextBox } from './expiry-date-text-box';

# `Expiry date text box`

 
Field used to display expiry date

## Validation 
- This field is disabled and populated from DB
- Max length is 10
- Field is numeric
- Masked as (  /  /    )
- Format is DD/MM/YYYY

## Attributes of Expiry date text box
Below is the list of Attributes.
<ArgsTable of={ ExpiryDateTextBox } /> 

## Events Description

### ExpiryDateTextBox-onChange

The onChange event occurs when the user change in text box

### ExpiryDateTextBox-onBlur

The onBlur event occurs before focus lost.

### ExpiryDateTextBox-onKeyPress

The onkeypress event occurs when the user presses a key (on the keyboard).

### ExpiryDateTextBox-onKeyUp

The onkeypress event occurs when the user presses a up-arrow key (on the keyboard).

### ExpiryDateTextBox-onFocus

The onfocus event occurs when an element gets focus.The onfocus event is most often used with input and select.
### Demo

<Canvas>
<Story id="expirydatetextbox--primary" />
</Canvas>

 