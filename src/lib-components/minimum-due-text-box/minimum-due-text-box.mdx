<!-- Custom-MDX-Documentation.mdx -->
import { ArgsTable , Canvas , Meta ,Story } from "@storybook/addon-docs";
import { MinimumDueTextBox } from './minimum-due-text-box';

# `Minimum Due text box`

Minimum Due is a Text Box Component is the minimum amount you are required to pay, on or before the payment due date

## Validation 
- Max 18 digits befor decimal and 2 digits after decimal
- Field data type is numericDecimal

## Attributes of Minimum Due text box
Below is the list of Attributes.
<ArgsTable of={ MinimumDueTextBox } /> 

## Events Description
Notes:
#
`1. None of events will occur if component is disabled`
### MinimumDueTextBox-onChange

The onChange event occurs when the user change in text box

### MinimumDueTextBox-onBlur

The onBlur event occurs before focus lost.

### MinimumDueTextBox-onKeyPress

The onkeypress event occurs when the user presses a key (on the keyboard).

### MinimumDueTextBox-onKeyUp

The onkeypress event occurs when the user presses a up-arrow key (on the keyboard).

### DueDateTextBox-onFocus

The onfocus event occurs when an element gets focus.The onfocus event is most often used with input and select.
### Demo

<Canvas>
<Story id="minimumduetextbox--primary" />
</Canvas>

 