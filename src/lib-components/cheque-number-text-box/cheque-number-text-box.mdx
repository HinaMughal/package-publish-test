<!-- Custom-MDX-Documentation.mdx -->
import { ArgsTable , Canvas , Meta ,Story } from "@storybook/addon-docs";
import { ChequeNumberTextBox } from './cheque-number-text-box';

# `Cheque number text box`

 Cheque Number is text box component that indicates a number assigned to cheque book leave for cheque payments
 
## Validation 
- This field is mandatory
– Field value must be greater then 0
– Field is mandatory
– max length is 10 
– field is numeric

## Attributes of Cheque number text box
Below is the list of Attributes.
<ArgsTable of={ ChequeNumberTextBox } /> 

## Events Description

### ChequeNumberTextBox-onChange

The onChange event occurs when the user change in text box

### ChequeNumberTextBox-onBlur

The onBlur event occurs before focus lost.

### ChequeNumberTextBox-onKeyPress

The onkeypress event occurs when the user presses a key (on the keyboard).

### ChequeNumberTextBox-onKeyUp

The onkeypress event occurs when the user presses a up-arrow key (on the keyboard).

### ChequeNumberTextBox-onFocus

The onfocus event occurs when an element gets focus.The onfocus event is most often used with input and select.
### Demo

<Canvas>
<Story id="chequenumbertextbox--primary" />
</Canvas>

 