<!-- Custom-MDX-Documentation.mdx -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { RefNumberTranNumberTextBox } from "./ref-number-tran-number-text-box";

# `Ref number tran number text box`

This textbox is used to enter Ref/Trans Number.

## Validation

1. Field is required.
2. Max Length is 5.
3. DataType is AlphaNumericSpecial.

## Attributes of Ref number tran number text box

Below is the list of Attributes.

<ArgsTable of={RefNumberTranNumberTextBox} />{" "}

## Events Description

### RefNumberTranNumberTextBox-onChange

The onChange event occurs when the user change in text box

### RefNumberTranNumberTextBox-onBlur

The onBlur event occurs before focus lost.

### RefNumberTranNumberTextBox-onKeyPress

The onkeypress event occurs when the user presses a key (on the keyboard).

### RefNumberTranNumberTextBox-onKeyUp

The onkeypress event occurs when the user presses a up-arrow key (on the keyboard).

### RefNumberTranNumberTextBox-onFocus

The onfocus event occurs when an element gets focus.The onfocus event is most often used with input and select.

### Demo

<Canvas>
  <Story id="refnumbertrannumbertextbox--primary" />
</Canvas>
