<!-- Custom-MDX-Documentation.mdx -->
import { ArgsTable , Canvas , Meta ,Story } from "@storybook/addon-docs";
import { AccountTypeTextBox } from './account-type-text-box';

# `Account type text box`

 
Text box used to display account type

## Validation 
- Field is Disabled
- Field Left aligned
- Field is Alphanumeric

## Attributes of Account type text box
Below is the list of Attributes.
<ArgsTable of={ AccountTypeTextBox } /> 

## Events Description

### AccountTypeTextBox-onChange

The onChange event occurs when the user change in text box

### AccountTypeTextBox-onBlur

The onBlur event occurs before focus lost.

### AccountTypeTextBox-onKeyPress

The onkeypress event occurs when the user presses a key (on the keyboard).

### AccountTypeTextBox-onKeyUp

The onkeypress event occurs when the user presses a up-arrow key (on the keyboard).

### AccountTypeTextBox-onFocus

The onfocus event occurs when an element gets focus.The onfocus event is most often used with input and select.
### Demo

<Canvas>
<Story id="accounttypetextbox--primary" />
</Canvas>

 