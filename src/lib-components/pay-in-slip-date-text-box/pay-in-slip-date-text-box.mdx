<!-- Custom-MDX-Documentation.mdx -->
import { ArgsTable , Canvas , Meta ,Story } from "@storybook/addon-docs";
import { PayInSlipDateTextBox } from './pay-in-slip-date-text-box';

# `Pay in slip date text box`

 
This field is used to insert Pay in slip date

## Validation 
- This field is mandatory
- Max length is 10
- Field is numeric
- Masked as (  /  /    )
- Format is DD/MM/YYYY

## Attributes of Pay in slip date text box
Below is the list of Attributes.
<ArgsTable of={ PayInSlipDateTextBox } /> 

## Events Description

### PayInSlipDateTextBox-onChange

The onChange event occurs when the user change in text box

### PayInSlipDateTextBox-onBlur

The onBlur event occurs before focus lost.

### PayInSlipDateTextBox-onKeyPress

The onkeypress event occurs when the user presses a key (on the keyboard).

### PayInSlipDateTextBox-onKeyUp

The onkeypress event occurs when the user presses a up-arrow key (on the keyboard).

### PayInSlipDateTextBox-onFocus

The onfocus event occurs when an element gets focus.The onfocus event is most often used with input and select.
### Demo

<Canvas>
<Story id="payinslipdatetextbox--primary" />
</Canvas>

 