<!-- Custom-MDX-Documentation.mdx -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { SignatoryInformationTextBox } from "./signatory-information-text-box";

# `Signatory information text box`

This textbox is used to input Signatory Information.

## Validation

1. Field is Mandatory.
2. DataType is AlphaSpecial.
3. Max Length is 30.

## Attributes of Signatory information text box

Below is the list of Attributes.

<ArgsTable of={SignatoryInformationTextBox} />{" "}

## Events Description

### SignatoryInformationTextBox-onChange

The onChange event occurs when the user change in text box

### SignatoryInformationTextBox-onBlur

The onBlur event occurs before focus lost.

### SignatoryInformationTextBox-onKeyPress

The onkeypress event occurs when the user presses a key (on the keyboard).

### SignatoryInformationTextBox-onKeyUp

The onkeypress event occurs when the user presses a up-arrow key (on the keyboard).

### SignatoryInformationTextBox-onFocus

The onfocus event occurs when an element gets focus.The onfocus event is most often used with input and select.

### Demo

<Canvas>
  <Story id="signatoryinformationtextbox--primary" />
</Canvas>
